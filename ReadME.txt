the following assignment of dynamic graph creation interface is done on react
plz type the following command on terminal in the root directory containing src folder to install the node modules dependencies and react-chartjs-2 dependencies
*  npm i
* npm i react-chartjs-2

features of the app
user selects types(bar, line or Doughnut type chart)
labels for x axis and y axis
n number of datapoints values(as required by user)
